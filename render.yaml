services:
  - type: web
    name: pulsenet-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PULSENET_SECRET
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.0
